1.升级分区是在boot里面写死，还是根据上位机下发参数决定好？这个关系到boot代码是否能同芯片能够复用。
还是说每个产品哪怕同芯片也是单独一个boot。另外如果只是同产品用同一个boot的话，写死是否有风险？

2.boot如何获取app版本？通过noinit段传递，还是通过固定flash地址传递。smota作为ota框架的话，不应该
限制获取app版本的方式，是不是需要在port写一个回调函数支持该操作？

3.boot如何决定跳转时机？我希望是boot完成初始化之后，默认X秒然后